meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"

build:
  requirements: requirements.txt

run:
  command: gunicorn -b 0.0.0.0:80 app:app
  containerPort: 80
  persistenceMount: /app/static
